import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// real index file

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
